package page


import (
 "uot-exam/internal/domain"
 "uot-exam/web/templates/pages/admin_dashboard/components"
 "uot-exam/web/templates/components/navbar"
 "uot-exam/web/templates/components/sidebar"
 "uot-exam/web/templates/components/collapsible"
 "uot-exam/web/templates/components/dropdown"
 "uot-exam/web/templates/components/popover"
 "uot-exam/web/templates/components/toast"
 "uot-exam/web/templates/components/chart"
 "uot-exam/web/templates/components/icon"
 "uot-exam/web/templates/components/tabs"
 "uot-exam/web/templates/components/input"
 "uot-exam/web/templates/components/textarea"
 "uot-exam/web/templates/components/card"
 "uot-exam/web/templates/components/label"
 "uot-exam/web/templates/utils"
 "uot-exam/web/templates/components/button"
 "strconv"
 "uot-exam/web/templates/components/badge"
 "uot-exam/web/templates/components/selectbox"
 "uot-exam/web/templates/components/dialog"
)

type EditExamPageParam struct {
	Username string
	FullName string
	Subject  domain.Subject
	Instructor domain.User
	Exam domain.Exam
	Questions []domain.Question
	Choices []domain.Choice
}

templ EditExamPage(param EditExamPageParam) {

	        <!-- KaTeX CSS -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css" 
              integrity="sha384-n8MVd4RsNIU0tAv4ct0nTaAbDJwPJzDEaqSD1odI+WdtXRGWt2kTvGFasHpSy3SV" 
              crossorigin="anonymous"/>
        
        <!-- KaTeX JS -->
        <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js" 
                integrity="sha384-XjKyOOlGwcjNTAIQHIpgOno0Hl1YQqzUOEleOLALmuqehneUG+vnGctmUb0ZY0l8" 
                crossorigin="anonymous"></script>
        
        <!-- KaTeX Auto-render Extension -->
        <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js" 
                integrity="sha384-+VBxd3r6XgURycqtZ117nYw44OOcIax56Z4dCRWbxyPt0Koah1uHoK0o4+/RRE05" 
                crossorigin="anonymous"
                onload="renderMathInElement(document.body, {
                    delimiters: [
                        {left: '$$', right: '$$', display: true},
                        {left: '$', right: '$', display: false},
                        {left: '\\[', right: '\\]', display: true},
                        {left: '\\(', right: '\\)', display: false}
                    ],
                    throwOnError: false
                });"></script>


        <!-- Highlight.js CSS (GitHub Dark theme) -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github-dark.min.css"/>
        
        <!-- Highlight.js JavaScript -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>


    @sidebar.Script()
	@tabs.Script()
	@collapsible.Script()
	@dropdown.Script()
	@popover.Script()
	@dialog.Script()
    @toast.Script()
    @input.Script()
	@selectbox.Script()
	@textarea.Script()
	@chart.Script()
	@label.Script()
	<script>
            // Highlight all code blocks on page load
            hljs.highlightAll();
            
            // Re-highlight after HTMX updates
            document.body.addEventListener('htmx:afterSwap', function(event) {
                event.detail.target.querySelectorAll('pre code').forEach((block) => {
                    hljs.highlightElement(block);
                });
            });
        </script>

    @components.Sidebar("/admin/dashboard/subjects", []navbar.BreadPath{
        {
            Href: "/admin/dashboard",
            Path: "Dashboard",
        },
        {
            Href: "/admin/dashboard/subjects",
            Path: "Subjects",
        },
        {
            Href: "/admin/dashboard/subject/" + param.Subject.ID.String(),
            Path: param.Subject.Title,
        },
		{
			Href: "/admin/dashboard/subject/" + param.Subject.ID.String() + "/exam/" + param.Exam.ID.String(),
			Path: param.Exam.Title,
		},
    }, param.Username, param.FullName) {

     			<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-500">
    				<div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
					<div>
						<h1 class="text-3xl font-extrabold text-slate-900 tracking-tight flex items-center gap-3">
							@icon.Book(icon.Props{Class: "h-8 w-8"})
							{param.Exam.Title}
						</h1>
						<p class="text-slate-500 mt-1">Edit exam information</p>
					</div>
               </div>
				@TabsExam(param)
			</div>
			<div id="toast-container"></div>
		}
	}

templ TabsExam(param EditExamPageParam) {
	@tabs.Tabs(tabs.Props{
		ID: "account-tabs",
	}) {
		@tabs.List(tabs.ListProps{
			Class: "w-full max-w-full",
		}) {
			@tabs.Trigger(tabs.TriggerProps{
				Value: "questions",
				IsActive: true,
                TabsID:   "account-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Questions
			}

			@tabs.Trigger(tabs.TriggerProps{
				Value: "students",
                TabsID:   "account-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Students	
			}

			@tabs.Trigger(tabs.TriggerProps{
				Value:    "analytics",
                TabsID:   "account-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Exam Analytics
			}

			@tabs.Trigger(tabs.TriggerProps{
				Value:    "information",
                TabsID:   "account-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Exam Information
			}
		}
		<div class="w-full mt-2">
			@tabs.Content(tabs.ContentProps{
				Value:    "information",
                TabsID:   "account-tabs",
			}) {
				@ExamInformationTab(param)
			}
			@tabs.Content(tabs.ContentProps{
				Value:    "questions",
                TabsID:   "account-tabs",
			}) {
				@QuestionsTab(param)
			}
			@tabs.Content(tabs.ContentProps{
				Value:    "students",
                TabsID:   "account-tabs",
			}) {
                // TODO: Implement StudentsTab
			}
			@tabs.Content(tabs.ContentProps{
				Value:    "analytics",
                TabsID:   "account-tabs",
			}) {
                // TODO: Implement AnalyticsTab
			}
		</div>
	}
}

templ ExamInformationTab(param EditExamPageParam) {
	@card.Card() {
		@card.Header() {
			@card.Title() {
				Exam Information 
			}
			@card.Description() {
				Make changes to your exam here. Click save when you are done.
			}
		}
		<form id="edit_exam_form"
			hx-post={ "/admin/dashboard/exam/edit/" + param.Exam.ID.String() }
			hx-target="#toast-container"
			hx-swap="none">
		@card.Content() {
			<div class="grid grid-cols-2 gap-4">
			<div>
			@label.Label(label.Props{
				For: "exam_name",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.Pen(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Name
			}

			@input.Input(input.Props{
				Type: input.TypeText,
				Placeholder: "Exam Name",
				ID: "exam_name",
				Name: "exam_name",
				Value: param.Exam.Title,
				Class: "mb-4",
			})
			</div>
			<div>
			@label.Label(label.Props{
				For: "exam_uuid",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.Hash(icon.Props{
					Class: "h-4 w-4",
				})
				Exam UUID 
			}
			@input.Input(input.Props{
				Type: input.TypeText,
				ID: "exam_uuid",
				Value: param.Exam.ID.String(),
				Readonly: true,
				Disabled: true,
				Class: "mb-4",
			})
			</div>
			
			<div>
			@label.Label(label.Props{
				For: "exam_duration",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.Clock(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Duration 
			}
			@input.Input(input.Props{
				Type: input.TypeNumber,
				ID: "exam_duration",
				Name: "exam_duration",
				Value: strconv.Itoa(int(param.Exam.DurationMinutes)),
				Class: "mb-4",
				Attributes: templ.Attributes{
					"min": "0",
				},
			})
			</div>
			<div>
			@label.Label(label.Props{
				For: "exam_subject_uuid",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.Book(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Subject UUID 
			}
			@input.Input(input.Props{
				Type: input.TypeText,
				ID: "exam_subject_uuid",
				Value: param.Exam.SubjectID.String(),
				Readonly: true,
				Disabled: true,
				Class: "mb-4",
			})
			</div>
			<div>
			@label.Label(label.Props{
				For: "exam_pass_score",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.Trophy(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Pass Score 
			}
			@input.Input(input.Props{
				Type: input.TypeNumber,
				ID: "exam_pass_score",
				Name: "exam_pass_score",
				Value: strconv.Itoa(int(param.Exam.PassScore)),
				Class: "mb-4",
				Attributes: templ.Attributes{
					"min": "0",
				},
			})
			</div>
			<div>
			@label.Label(label.Props{
				For: "exam_created_by",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.User(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Created By 
			}
			@input.Input(input.Props{
				Type: input.TypeText,
				ID: "exam_created_by",
				Value: param.Exam.CreatedBy.String(),
				Readonly: true,
				Disabled: true,
				Class: "mb-4",
			})
			</div>

			<div>
			@label.Label(label.Props{
				For: "exam_total_marks",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.BadgeCheck(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Total Marks 
			}
			@input.Input(input.Props{
				Type: input.TypeNumber,
				ID: "exam_total_marks",
				Name: "exam_total_marks",
				Value: strconv.Itoa(int(param.Exam.TotalMarks)),
				Class: "mb-4",
				Attributes: templ.Attributes{
					"min": "0",
				},
			})
			</div>
			<div>
			@label.Label(label.Props{
				For: "exam_status",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.BadgeCheck(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Status 
			}
		@selectbox.SelectBox() {
			@selectbox.Trigger(selectbox.TriggerProps{
				Name: "exam_status",
			}) {
				@selectbox.Value(selectbox.ValueProps{
					Placeholder: "Select the status",
				})
			}
			@selectbox.Content(selectbox.ContentProps{NoSearch: true}) {
				@selectbox.Group() {
					@selectbox.Label() {
						Status
					}
					@selectbox.Item(selectbox.ItemProps{
						Value: "DRAFT",
						Selected: string(param.Exam.Status) == "DRAFT",
						Class: "mb-2 mt-2",
					}) {
						@badge.Badge(badge.Props{
							Variant: badge.VariantSecondary,
						}) {
							Draft
						}
					}
					@selectbox.Item(selectbox.ItemProps{
						Value: "PUBLISHED",
						Selected: string(param.Exam.Status) == "PUBLISHED",
						Class: "mb-2",
					}) {
						@badge.Badge(badge.Props{
							Variant: badge.VariantDefault,
						}) {
							Published
						}
					}
					@selectbox.Item(selectbox.ItemProps{
						Value: "ARCHIVED",
						Selected: string(param.Exam.Status) == "ARCHIVED",
						Class: "mb-2",
					}) {
						@badge.Badge(badge.Props{
							Variant: badge.VariantDestructive,
						}) {
							Archived
						}
					}
				}
			}
		}
			</div>
			</div>
			@label.Label(label.Props{
				For: "exam_description",
				Class: "flex items-center gap-2 mb-2 mt-4",
			}) {
				@icon.MessageSquareText(icon.Props{
					Class: "h-4 w-4",
				})
				Exam Description
			}

			@textarea.Textarea(textarea.Props{
				Placeholder: "Exam Description",
				ID: "exam_description",
				Name: "exam_description",
				Rows: 3,
				Value: utils.IfElse(param.Exam.Description != nil, *param.Exam.Description, ""),
				Class: "mb-4",
			})
		}
		@card.Footer(card.FooterProps{
			Class: "flex justify-end gap-2",
		}) {
			@button.Button(button.Props{
				Type: button.TypeButton,
				Class: "bg-red-500 hover:bg-red-600",
				Attributes: templ.Attributes{
					"hx-post": "/admin/dashboard/exam/delete/" + param.Exam.ID.String(),
					"hx-target": "#toast-container",
					"hx-swap": "none",
				},
			}) {
				Delete Exam
			}

			@button.Button(button.Props{
				Type: button.TypeSubmit,
				Form: "edit_exam_form",
				
			}) {
				Save changes
			}
		}
		</form>
	}
}

templ QuestionsTab(param EditExamPageParam) {

<div class="grid grid-cols-2 gap-4 mb-4">
@components.TotalCard(components.TotalCardProps{
	Title: "Total Questions",
	Description: "Total number of questions in this exam.",
	Icon: icon.BadgeQuestionMark(icon.Props{
		Class: "h-10 w-10",
	}),
	Value: "5",
})
</div>
	@card.Card() {
		@card.Header() {
			@card.Title() {
				<div class="flex items-center gap-2 justify-between w-full">
				Questions
				@AddQuestionButton(param)
			</div>
			@card.Description() {
				create questions for your exam here.
			}
			}
		}
		@card.Content() {
			@components.QuestionList(components.QuestionListProps{
				Questions: param.Questions,
				Choices: param.Choices,
			})
		}
	}
}

templ AddQuestionButton(param EditExamPageParam) {
	@dialog.Dialog(){
		@dialog.Trigger(){
			@button.Button(button.Props{
				Type: button.TypeButton,
				Variant: button.VariantDefault,
				Attributes: templ.Attributes{
					"hx-post": "/admin/dashboard/exam/question/add/" + param.Exam.ID.String(),
					"hx-target": "#toast-container",
					"hx-swap": "none",
				},
			}) {
				Add Question
			}
		}

		@dialog.Content(){
			@dialog.Header(){
				@dialog.Title(){
					Add Question
				}
				@dialog.Description(){
					create questions for your exam here.
				}
			}
				@AddQuestionDialog(param)
		}
	}
	}

	templ AddQuestionDialog(param EditExamPageParam) {
			@tabs.Tabs(tabs.Props{
		ID: "add-question-tabs",
	}) {
		@tabs.List(tabs.ListProps{
			Class: "w-full max-w-full",
		}) {
			@tabs.Trigger(tabs.TriggerProps{
				Value: "Text",
				IsActive: true,
                TabsID:   "add-question-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Text
			}

			@tabs.Trigger(tabs.TriggerProps{
				Value: "Code",
                TabsID:   "add-question-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Code	
			}

			@tabs.Trigger(tabs.TriggerProps{
				Value:    "Image",
                TabsID:   "add-question-tabs",
                Class: "data-[tui-tabs-state=active]:bg-primary data-[tui-tabs-state=active]:text-primary-foreground",
			}) {
				Image
			}
		}
		<div class="w-full mt-2">
			@tabs.Content(tabs.ContentProps{
				Value:    "Text",
                TabsID:   "add-question-tabs",
			}) {
				@components.TextQuestionForm(domain.Question{})
			}
			@tabs.Content(tabs.ContentProps{
				Value:    "Image",
                TabsID:   "add-question-tabs",
			}) {
				@components.ImageQuestionForm(domain.Question{})
			}
			@tabs.Content(tabs.ContentProps{
				Value:    "Code",
                TabsID:   "add-question-tabs",
			}) {
                // TODO: Implement StudentsTab
			}
		</div>
	}
	}